# Listas para automação
files = ["log1.txt", "log2.txt"]
commands = ["ls", "pdw", "date"]

# Dicionários para configuração
config = {
    "host": "localhost",
    "port": 8080,
    "auto_restart": True
}